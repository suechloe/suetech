# ═══════════════════════════════════════════════════════
# Sue Tech 项目依赖
# 安装: pip install -r requirements.txt
# 兼容: Python 3.9+
# ═══════════════════════════════════════════════════════

# ── Web 框架 (Dashboard + API) ──
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# ── LLM API ──
openai>=1.0.0            # DeepSeek（兼容 OpenAI 接口）
anthropic>=0.25.0         # Claude（可选，Sage 代码增强）

# ── 飞书 SDK ──
lark-oapi>=1.3.0

# ── 工具库 ──
python-dotenv>=1.0.0      # .env 环境变量
httpx>=0.24.0             # 异步 HTTP（余额查询等）
pydantic>=2.0.0           # 数据校验
